# PB Valley Web (Server ภายในบริษัท)

## ชื่อเว็บภายในบริษัท — ตั้งค่าล่าสุด

- เปิด `Start Web.cmd` จะเปิดเว็บจาก Server เดิมถ้ารันอยู่ หรือเริ่ม Server พอร์ต 80 ในเบื้องหลัง ไม่ต้องเปิดซ้ำหลายหน้าต่าง
- เครื่อง Server เปิด http://www.pbvalleystock.com ได้แล้ว (hosts เฉพาะเครื่องนี้)
- เครื่องอื่นในวง LAN เปิด http://192.168.0.100 ได้ ไม่ต้องใส่ :3080
- การตั้ง Firewall ใช้กฎ `PBValleyStock-LAN-HTTP` เฉพาะ Node.js / TCP 80 / IP เครื่อง 192.168.0.100 / ผู้เชื่อมต่อ 192.168.0.0/24
- `Setup Company Web.cmd` ใช้ตั้ง hosts และ Firewall ใหม่ด้วยสิทธิ์ผู้ดูแล หากย้ายเครื่องต้องตรวจ IP ก่อน
- เก็บ hosts เดิมไว้ใน `web-logs/hosts-before-*.txt` ไม่มีการลบกฎเครือข่ายเดิม
- Server ยังไม่ถูกตั้งให้เริ่มเมื่อเปิด Windows ให้เปิด `Start Web.cmd` หลังรีสตาร์ตเครื่อง

### ขั้นตอนที่ต้องทำที่เราเตอร์/DNS บริษัท

1. จอง DHCP ให้คอม Server ได้ IP 192.168.0.100 เสมอ (ยังไม่ได้เปลี่ยนค่าที่เราเตอร์)
2. เพิ่ม Local DNS / Host override: `www.pbvalleystock.com` และ `pbvalleystock.com` → `192.168.0.100`
3. แจก DNS ภายในนั้นให้เครื่องลูกข่ายผ่าน DHCP และเชื่อม Wi-Fi ใหม่
4. เครื่องที่กำหนด DNS Google 8.8.8.8 เองต้องเปลี่ยนมาใช้ DNS ภายในเพื่อให้รู้จักชื่อส่วนตัวนี้

เราเตอร์ที่พบคือ 192.168.0.1 ยังไม่ได้เข้าระบบหรือแก้การตั้งค่า DNS ทั้งบริษัท
ไม่ต้องเพิ่ม port forwarding เพราะใช้เฉพาะในบริษัท

## เปิดใช้งาน

1. ใช้คอมเครื่องกลางที่ติดตั้ง Node.js 22.13 ขึ้นไป (เครื่องนี้มีแล้ว)
2. ดับเบิลคลิก `Start Web.cmd` ในโฟลเดอร์โปรเจกต์ เปิดหน้าต่างนี้ทิ้งไว้
3. บนคอม Server เปิด http://localhost:3080 ตั้งรหัสผ่านครั้งแรกอย่างน้อย 12 ตัวอักษร
4. มือถือ/คอมอื่นเชื่อม Wi-Fi/LAN เดียวกัน แล้วเปิด URL บรรทัด `LAN:` ที่แสดงในหน้าต่าง Server
5. ถ้าเครื่องอื่นเข้าไม่ได้ ให้ผู้ดูแลอนุญาต TCP 3080 ใน Windows Firewall เฉพาะเครือข่าย Private ของบริษัท

รหัสผ่านรุ่นนี้เป็นบัญชีผู้ดูแลร่วมกัน ผู้ที่มีรหัสสามารถเพิ่ม แก้ไข ลบ และตั้งค่า Bot ได้ ยังไม่มีบัญชีและสิทธิ์รายบุคคล
ระบบอัปเดตข้อมูลจาก Server ทุก 15 วินาที และเมื่อกลับมายังหน้าต่าง ปุ่มเพิ่ม–ลดปรับจำนวนจากยอดล่าสุดที่ Server
หากมีคนแก้สินค้าขณะอีกคนเปิดฟอร์ม ระบบปฏิเสธการบันทึกข้อมูลเก่าทับข้อมูลใหม่ ให้ปิดฟอร์ม รอข้อมูลอัปเดต แล้วเปิดอีกครั้ง

## ข้อมูลเดิม

ข้อมูลเว็บเก็บใน `web-data/stock-tracker.sqlite` ส่วนข้อมูลแอปเดิมยังอยู่ที่เดิม
ย้ายครั้งแรกเท่านั้น ก่อนมีฐานข้อมูลเว็บ:

```powershell
node web-dist/server.cjs --import-db "C:\Users\natta\AppData\Roaming\stock-expiration-tracker\stock-tracker.sqlite"
```

คำสั่งคัดลอกฐานข้อมูลแบบ SQLite backup จึงเก็บรูป หมวดหมู่ และสินค้าไว้ครบ ไม่เขียนต้นฉบับและไม่เขียนทับฐานข้อมูลเว็บที่มีอยู่
Token จากแอปผูกกับการเข้ารหัส Windows จึงปิดการแจ้งเตือนในสำเนาเว็บและล้าง Token เฉพาะสำเนา ให้ตั้ง Token ใหม่ในเว็บ
หยุดใช้ตัวแจ้งเตือนในแอปเดิมก่อนเปิดการแจ้งเตือนเว็บ เพื่อไม่ให้ส่งซ้ำจากสองโปรแกรม

## แจ้งเตือนและสำรองข้อมูล

- ตั้ง LINE / Telegram และเวลาที่เมนูตั้งค่าระบบ บันทึกแล้วใช้ปุ่มทดสอบได้
- Server ต้องเปิดและไม่ Sleep เพื่อให้ทุกคนใช้เว็บและส่งข้อความตามเวลาได้ การปิดแท็บเบราว์เซอร์ไม่หยุด Server
- ปิด Server ด้วย Ctrl+C ก่อนสำรองโฟลเดอร์ `web-data` ทั้งโฟลเดอร์ไปยังไดรฟ์สำรอง
- ต้องเก็บ `server.json` คู่กับฐานข้อมูล เพราะมีค่าเข้ารหัส Token และรหัสผ่านแบบ hash อย่าแชร์ไฟล์นี้
- ย้าย Server ให้คัดลอกโฟลเดอร์โปรเจกต์และ `web-data` ลงคอมใหม่ ติดตั้ง Node.js และรัน `npm ci` แล้ว `npm run build:web`

## ขอบเขตเครือข่าย

รุ่นนี้สำหรับ LAN ที่ไว้ใจได้ ใช้ HTTP ภายใน ไม่ได้เผยแพร่ขึ้นอินเทอร์เน็ตและไม่ได้เปิด Firewall ให้อัตโนมัติ
หากใช้นอกบริษัท ต้องตั้ง VPN หรือ HTTPS reverse proxy ให้ผู้ดูแลก่อน ไม่เปิดพอร์ตนี้ออกอินเทอร์เน็ตตรง ๆ
เมื่อใช้ HTTPS ตั้ง `PB_HTTPS=1` และถ้าใช้ชื่อโดเมนเพิ่ม `PB_ALLOWED_HOSTS=ชื่อโดเมน` (ไม่ใส่ protocol หรือ port)
มือถือใช้ตารางแบบเลื่อนแนวนอนเพื่อรักษาข้อมูลและปุ่มจัดการครบ

## สำหรับผู้ดูแลโปรเจกต์

```powershell
npm run build:web
npm run start:web
```

กำหนด `PORT` (ค่าเริ่มต้น 3080), `HOST` (ค่าเริ่มต้น 0.0.0.0), `PB_DATA_DIR` เพื่อเปลี่ยนที่เก็บข้อมูลได้
ใช้ Node Server เพียงหนึ่ง process ต่อฐานข้อมูล ไม่ใช้ไฟล์ SQLite ผ่านโฟลเดอร์แชร์ ทุกเครื่องเข้าผ่าน HTTP API
สำหรับทดสอบใช้ `PB_DISABLE_SCHEDULER=1` เพื่อไม่ส่งข้อความจริง
